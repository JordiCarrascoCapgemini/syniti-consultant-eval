fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# psycopg 3, sync, with the connection pool and prebuilt binaries.
psycopg[binary,pool]>=3.1.0
argon2-cffi>=23.1.0
pytest>=8.0.0
# Required by starlette's TestClient.
httpx>=0.27.0
